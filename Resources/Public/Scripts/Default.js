$(function() {
    /*
    * handle frontend
    * */
    if (!$("body").hasClass("neos-backend")) {
        // nothing yet
    }
    /*
    * handle backend
    * */
    else {
        // nothing yet
    }
});